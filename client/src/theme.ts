import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0186DA",
    },
    background: {
      default: "#ffffff",
    },
  },
});

export default theme;
